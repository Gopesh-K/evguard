defaults:
  max_power_kw: 7.0
  max_current_a: 32.0

sequence:
  window_seconds: 10
  max_commands_per_source: 10

roles:
  controller:
    - CONNECT
    - AUTHORIZE_SESSION
    - START_CHARGING
    - SET_POWER
    - SET_CURRENT
    - STOP_CHARGING
    - DISCONNECT
  monitor: []
